# --- Logs ---
logs
*.log
npm-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# --- Node ---
node_modules/

# --- Builds / Caches ---
dist/
build/
.out/
.next/
.vercel/
.cache/
coverage/
node_modules/.vite/
*.tsbuildinfo

# --- Vite/Tooling (selten nötig, aber harmless) ---
.parcel-cache/
.turbo/
.eslintcache

# --- Env / local config ---
.env
.env.*
!.env.example
.npmrc
.envrc

# --- OS / Editor ---
.DS_Store
Thumbs.db
.idea/
.vscode/

# --- Project-specific ---
# Wenn dein Express-Server hier fertige, generierte Assets ablegt, ignoriere den Ordner.
# Entferne diese Zeile, falls du dort bewusst statische, versionierte Dateien ablegst.
server/public/

# Vite Config-Pattern entfernen – normalerweise willst du die Config versionieren
# (also KEIN ignore auf vite.config.ts)
